
String Encrypt(String s) {
  // String key = md5.convert(utf8.encode(SharedSecret)).toString();
  // var aesEncrypter = AesCrypt(key, 'cbc', 'pkcs7');
  // var iv = CryptKey().genDart(16);
  // var encPayload = aesEncrypter.encrypt(s, iv);
  // return base64
  //     .encode(Uint8List.fromList(iv.codeUnits) + base64.decode(encPayload));
  return s;
}
