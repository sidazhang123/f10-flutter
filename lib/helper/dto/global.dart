library my_prj.globals;

import 'dart:async';

StreamController<String> tab2panelStream = StreamController<String>();
StreamController<bool> panel2tabStream = StreamController<bool>();
StreamController<bool> header2panelStream =StreamController<bool>();
